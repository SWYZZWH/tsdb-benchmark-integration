targets:
  - db: timescaledb
    host: localhost
    port: "5432"
    pass: "password"
variant:
  name: batch-size
  values:
    - 10
invariants:
  use-case: devops
  scale: 1000
  workers: 4
  use-qps-limiter: true
  limiter-max-qps: 20000
  limiter-bucket-size: 40000
  expected-test-duration: 20
criterion:
  max-time-deviation-rate: 0.1
server:
  host: localhost
  port: 8123